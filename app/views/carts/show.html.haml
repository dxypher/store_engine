- current_cart.products.each do |product|
  %ul
    %li
      %ul
        %li= product.title
        %li= product.description
        %li= "Price: $#{product.price}"
        %li= link_to 'remove', cart_item_path(product.cart_items), method: :delete